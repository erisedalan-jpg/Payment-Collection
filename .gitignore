# ── 部署打包产物(大体积二进制+含数据,不入库) ──
pmplatform-deploy-*.zip
# 升级/更新包构建产物（含 dist，本机生成，不入库）
release/

# ── Python ──
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.venv/
venv/

# ── 打包产物 ──
build/
dist/

# ── 运行日志 ──
log/

# ── 生成物 / 抓取物 / 运行时数据（不入库，由脚本在本机生成）──
# analysis_data.json/.js 由 preprocess_data.py 生成（~2.2MB）；测试样本请放 tests/fixtures/
data/analysis_data.json
data/analysis_data.js
# yundocs_data/ 由 fetch_yundocs_full.py 抓取，含真实业务数据，不入库
yundocs_data/
# input/ 为用户放置/上传的真实数据（PMIS 七表、组织架构、项目映射、预算核算），不入库
input/
# 倚天导出（验证阶段）：cookie 凭据 + 导出数据不入库；脚本/说明可入库
yitian/config.json
yitian/downloads/
yitian/__pycache__/
# 快照与事件流（运行时生成，Phase P3）
data/snapshots/
data/events.json
# 数据历史版本（含真实源数据副本，运行时生成，不入库）
data/history/
# 人工数据导入快照（运行时生成，含本地用户数据，2E）
data/manual_backups/
# 本地跟进记录（运行时用户数据）
data/followup_records.json
# 项目标签库（运行时用户数据，2C）
data/project_tags.json
# 首页门户/快捷入口配置 + 上传文件（本地敏感数据，运行时生成，不入库）
data/portal_links.json
data/portal_files/
# 重点项目进展数据（运行时用户数据，SP-2）
data/project_progress.json
# 商机管理数据（运行时用户数据，V2.0.0）
data/opportunities.json
data/opportunities.backup-*.json
# 临时重点跟进数据(运行时用户数据,V2.1.0)
data/temp_followup.json
data/temp_followup.backup-*.json
# 重点商机跟进数据(运行时用户数据,V2.2.0)
# 【V4.5.9 补】本条此前【漏了】—— 隔壁 opportunities.json / risk_followup.json /
# payment_key_followup.json / temp_followup.json 四个同族都在,唯独它没进来,
# 于是磁盘上一份 100KB 的真实商机跟进记录一直裸露在未忽略状态(所幸从未入库)。
# 显式列举的代价就是这样:少写一行不会有任何报错,只会在某次 `git add .` 时发作。
data/opportunity_followup.json
data/opportunity_followup.json.tmp
# 运行时生成的带时间戳备份/临时文件。名字里带日期,【无法预先逐个列举】,
# 故此处用通配兜底(data/ 下没有任何文件是需要入库的)。
# V4.5.9 实测漏网例:data/temp_followup.json.bak-20260721-1010 ——
# 既有的 data/temp_followup.backup-*.json 与它的真实命名(.json.bak-<时间戳>)对不上。
data/*.bak-*
data/*.backup-*
data/*.tmp
# 本地账号库(含密码哈希,敏感,运行时生成,SP-2)
data/accounts.json
data/accounts.json.tmp
data/accounts.json.bak-*
data/audit_log.jsonl
data/audit_log.jsonl.tmp
data/audit_archive/
# 回款比对报告（含真实业务数据，一次性诊断产物，不入库）
report/

# ── 前端（Vue/Vite）──
frontend/node_modules/
frontend/dist/
frontend/*.tsbuildinfo
# schema.py 导出的中间产物（TS 类型生成用），不入库
schema.json
data/yitian_data.json
data/yitian_store.json
yitian_schema.json

# ── 系统 ──
.DS_Store
Thumbs.db

# ── 工具产物 ──
# brainstorming 可视化草图（本机临时）
.superpowers/
.vite/
# oh-my-claudecode 运行时状态/检查点（本机临时，不入库）
.omc/
# 根级 node_modules（若有）
/node_modules/

client/agent_config.json
data/yitian_config.json
data/yitian_config.json.tmp
data/yitian_settings.json
data/yitian_settings.json.tmp
data/yitian_rules.json
data/yitian_rules.json.tmp
# 概算工具(V3.1.0):费率配置 = 对外销售单价;报价存档 = 客户名称/项目金额/销售姓名/整份报价明细。
# 都是对外商务数据,绝不入库(一次 git add -A 就洗不掉了)。
data/budget_config.json
data/budget_config.json.tmp
data/budget_estimates.json
data/budget_estimates.json.tmp
# 补齐遗漏(V2.3.0/V2.6.x 起就一直没被忽略):跟进数据含真实项目号与跟进正文,同属不入库之列。
data/risk_followup.json
data/risk_followup.json.tmp
data/payment_key_followup.json
data/payment_key_followup.json.tmp
# 跟进表自定义列配置(超管可配,含列名;按既有惯例数据/配置不入库)
data/followup_columns.json
data/followup_columns.json.tmp
# client 本地敏感/用户态/产物:config.json 含真实 cookie(密钥)、gui_settings 含账号设置、dist 为打包产物
client/config.json
client/gui_settings.json
client/dist/

# LTS 精简副本(lts/)的构建产物与运行数据(源码入库,产物/数据不入库)
lts/frontend/node_modules/
lts/frontend/dist/
lts/data/
lts/input/
lts/release/
lts/build/
lts/**/__pycache__/
lts/*.zip
# 蓝信推送配置(含 AppSecret,绝不入库)
data/lanxin_config.json
data/lanxin_config.json.tmp
# 蓝信收件箱(含员工真实回复正文)与回调原始报文存证,均为运行数据,绝不入库
data/lanxin_inbox.json
data/lanxin_inbox.json.tmp
data/lanxin_callback_raw.jsonl
# 存证滚动归档(I-2):内容与上面的活动文件同性质,同样绝不入库
data/lanxin_raw_archive/

# ── 非平台组成部分:临时 PMIS 抓数脚本 + 散落数据/产物(含真实 cookie/业务数据,绝不入库) ──
# pmisdata/ 是临时 PMIS 抓数脚本(config.json 含真实会话 cookie、A.xlsx/_wbs_id_map.json 为真实项目数据),
# 非平台组成部分。2026-07-21 接入 GitHub 前已用 git filter-repo 从 lts/pmisdata/ 全历史清除。
pmisdata/
lts/pmisdata/
# 例外(V4.5.19):pmisdata/run_pmis_pipeline.py 是【本次新写的编排脚本】,只有备份/顺序/
# 校验/拷贝逻辑,不含任何 PMIS 接口细节与业务数据,已用 `git add -f` 纳入版本管理 ——
# 它被打进 exe、成了平台的一部分,没有版本记录就意味着别人 clone 后打不出可用的包。
# 已跟踪的文件不受 ignore 规则影响,故这里不写 `!` 例外(父目录被排除时 `!` 也不生效)。
# 另三个抓数脚本(fetch_*/delivery_analysis)仍不入库:含 PMIS 内部接口路径,
# 是否公开需用户拍板;打包时从本地磁盘取。
lts/frontend/*.tsbuildinfo
# 散落在工作树的一次性产物/实验目录,不入库
client.zip
oppoboard.pdf
demo.html
CostBudgetEstimate.html
nul
# 必须带前导 / 锚定仓库根:不带锚点的 `yitian/` 会匹配任意深度的同名目录,
# 把 frontend/src/lib/yitian/ 也一并忽略掉(V4.5.4 实际踩过:derived.ts 被静默拒收,
# 而引用它的 detail.ts 已提交 → 提交树在全新 clone 上构建必失败)。
/yitian-analyze/
/yitian/
/yitian-new/
/项目数据运营工具/

# 新部门交付用的真实业务数据(PMIS 导出 + 组织架构花名册,2026-08-30 放入)。
# 【必须带前导斜杠】与上面同理。发现经过:git check-ignore -v new-dep/ 报了个
# 「.gitignore:184: new-dep/」看着像已覆盖,但 184 行其实是【空行】、全文件没有这条规则 ——
# 对带尾斜杠的目录名 check-ignore 会给假阳性。逐文件 check-ignore 与 git status 两路
# 交叉验证才是准的(同类误判本仓已栽三次)。
/new-dep/

# 蓝信对接副本(他人基于 LTS-1.1.0 制作的督办系统整份复制,含 server.py/projects.py/frontend/tests)。
# 非本平台组成部分,只读参考、绝不入库;根 conftest.py 的 collect_ignore 亦已含它。
# 【必须带前导斜杠】仓库里有三个叫 lanxin 的目录 —— 另两个是【已跟踪】的
# frontend/src/lib/lanxin/ 与 lts/frontend/src/lib/lanxin/。无锚点的 `lanxin/` 会三个全匹配,
# 于是今后往前端那个目录新增文件会被 git add 静默拒收(V4.5.4 的 yitian/ 原样重演)。
/lanxin/

# docs/superpowers/research/ —— 排障期从真实数据导出的画像/清单(项目编号、项目名称、
# 项目金额、项目经理姓名、客户与签约单位字段)。2026-08-02 全量审查时发现它自 2026-06-09
# 起就在 public 仓库里(19 个文件、去重 75 个真实项目号,另有一份 160 行 × 11 列的 xlsx),
# 当日已用 `git filter-repo --path docs/superpowers/research --invert-paths` 从全历史清除、
# 并把散落在文档与测试注释里的项目号统一替换为 QAGD-XX-REDACTED。
# 【本目录在本地保留作资料,但绝不入库】。今后写调研产物请放这里,不要放 specs/plans。
# 教训:2026-07-21 首次接入 GitHub 时只排查了 data/ input/ pmisdata/,docs/ 下同样会长出
# 真实数据 —— 判据不是「目录名像不像数据目录」,而是「文件内容是不是从生产导出来的」。
/docs/superpowers/research/

# docs/audit/ —— 系统审查报告,含「哪个写端点不在超管闸内」「哪条路径可越权拿全量数据」
# 这类安全弱点细节。本仓是 public,这类内容只留本地。
/docs/audit/

# 登录页角色立绘的草稿与截图评审产物(37 个文件:puppeteer 抓图脚本 shot.js/shot-dark.js/diag.js、
# 多轮裁切与遮罩的 png、以及带真实账号界面的登录页截图)。属于设计过程物,不是平台组成部分。
# 2026-08-31 审查发现:它们【既未跟踪也未被忽略】,在 public 仓库前裸露 —— CLAUDE.md §9 禁止
# `git add -A`,但那是纪律不是机制,一次手滑就进去了,而上次泄漏已经证明推出去洗不掉。
/avatar-drafts/
/wxt.png
/wangxutong.png

# report/ —— 系统审查报告。含真实项目编号、合同金额、各域指标实测值(全部从生产数据导出),
# 判据同 docs/superpowers/research/:看内容是不是从生产导出来的,不看目录名。只留本地。
/report/

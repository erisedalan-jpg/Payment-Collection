# ── Python ──
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.venv/
venv/

# ── 打包产物 ──
build/
dist/

# ── 运行日志 ──
log/

# ── 生成物 / 抓取物 / 运行时数据（不入库，由脚本在本机生成）──
# analysis_data.js 由 preprocess_data.py 生成（~2.2MB）；测试样本请放 tests/fixtures/
data/analysis_data.js
# yundocs_data/ 由 fetch_yundocs_full.py 抓取，含真实业务数据，不入库
yundocs_data/
# 本地跟进记录（运行时用户数据）
data/followup_records.json

# ── 系统 ──
.DS_Store
Thumbs.db

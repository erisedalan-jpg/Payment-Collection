# D2.5 审计地基修复 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复 `/impeccable audit` 暴露的三条 P1（暗色模式失效 / 下钻入口键盘不可达 / muted 文字对比度不足）与两项 P2（自适应基线 / side-stripe 边框 anti-pattern），范围限定在 D3–D10 不会重写的"留存"组件，为 D3 及以后打好地基。

**Architecture:** 三个全局地基件先行（`theme.css` token 调整 / 新增语义状态色与反白色 token / 全局 `v-activate` 键盘激活指令 / 自适应栅格工具类），随后逐个"留存页面/共享组件"把硬编码 hex 按统一映射表换成 token、给 `@click` 的非语义元素挂 `v-activate`、移除 side-stripe 左边框。底层业务计算（`lib/*`）一律不动，仅改样式与可达性。

**Tech Stack:** Vue3 `<script setup lang="ts">` + Element Plus + Pinia + Vitest(@vue/test-utils/jsdom)。样式走 D1 已建立的 CSS 变量体系（`src/styles/theme.css`：`:root` 浅色 / `html.dark` 深色）。

---

## 背景与范围

`/impeccable audit`（2026-06-08）评分 11/20。根因：旧 `app.js` 内联样式被逐字移植进 `.vue`——颜色写死、交互用 `div@click`、对比度沿用旧值。D1 已建好 token 与双主题地基，但正文区 37 个文件共 317 处硬编码颜色未接入，导致暗色模式在 shell 之外几乎全失效。

用户决策（2026-06-08）：
1. 三条 P1 全做，muted 对比度作为独立的全局 token 微调。
2. 暗色 token 化**只迁移留存的共享组件**;将被 D3–D10 重写/删除的页面级视图延后到对应 plan。
3. P2 做：自适应基线 + 去 side-stripe 边框。

### 本计划范围内（"留存"路径，需 token 化的文件）

共享件 / 全局：
- `frontend/src/components/DataTable.vue`（1 处）
- `frontend/src/components/ColumnFilter.vue`（7 处）
- `frontend/src/layout/FilterBar.vue`（2 处）

留存页面 + 其专属组件：
- 回款台账：`frontend/src/views/LedgerView.vue`（19）、`frontend/src/components/LedgerTable.vue`（12）
- 数据管理：`frontend/src/views/DataView.vue`（16）、`frontend/src/components/DataQualityTable.vue`（5）
- 关于产品：`frontend/src/views/AboutView.vue`（7）
- 临期跟进：`frontend/src/views/FollowupView.vue`（23）、`frontend/src/components/FollowupSignalRow.vue`（12）、`frontend/src/components/FollowupExpandModal.vue`（16）、`frontend/src/components/FuProjectRow.vue`（6）、`frontend/src/components/FuNodeTable.vue`（3）、`frontend/src/components/FollowupRecords.vue`（18）、`frontend/src/components/FollowupRecordForm.vue`（9）

`frontend/src/components/Modal.vue` 与 `frontend/src/components/DataDrillModal.vue` 已确认 0 处硬编码，无需改。

### 明确延后（D3–D10 重写/删除，本计划不碰）

- Dashboard 系（→ D3）：`views/DashboardView.vue`、`components/DashSummaryCards.vue`、`TierCards.vue`、`PendingBarChart.vue`、`OrgRanking.vue`、`DelayedTop.vue`
- Compare/Pm 系（→ D4，删除入口）：`views/CompareView.vue`、`components/CompareCards.vue`、`views/PmView.vue`、`components/PmRankingTable.vue`、`PmDrilldownModal.vue`
- Calendar 系（→ D7-9）：`views/CalendarView.vue`、`components/CalGrid.vue`、`CalNodeTable.vue`
- Analysis tab 系（→ D10）：`views/TierView.vue`、`components/TierNodesTab.vue`、`TierIntegrityTab.vue`、`ProjectsOverviewTab.vue`、`RiskTab.vue`、`PlanTab.vue`、`PlanBoard.vue`

> 上述延后文件里的硬编码颜色 / 键盘可达性 / side-stripe，由各自的 D3–D10 plan 在重做时按本计划确立的同一套 token 映射与 `v-activate` 模式一并处理。

---

## 颜色 Token 映射表（所有 token 化任务统一遵循）

把旧 hex 按"语义"映射到 token，**不要**按字面色值机械替换。同一 hex 在不同语境（背景 / 文字 / 描边 / 状态强调）可能映射到不同 token，见"语境"列。

| 旧 hex（含大小写变体） | 语境 | → 替换为 |
|---|---|---|
| `#fff` `#ffffff` | 作卡片/面板**背景** | `var(--card)` |
| `#fff` `#ffffff` | 作**彩色填充上的文字/图标**（反白前景） | `var(--on-accent)` |
| `#f8fafc` `#fbfcfe` `#fafafa` `#fcfcfd` | 次级面/表头底 | `var(--card2)` |
| `#eef2f8` | 页面底色 | `var(--bg)` |
| `#0f172a` `#1f2a3d` `#1f2937` | 主文本 | `var(--txt)` |
| `#334155` `#475569` `#5b6b85` | 次级文本 | `var(--sub)` |
| `#64748b` `#8c8c9e` `#93a1b8` `#94a3b8` | 弱化文本/计数/空态 | `var(--mut)` |
| `#cbd5e1` | 作**文字/图标**（如未激活态） | `var(--mut)` |
| `#cbd5e1` `#d4dbe8` | 作**描边/分隔** | `var(--line2)` |
| `#e2e8f0` `#e6eaf2` `#f1f5f9` `#ebe7e2` | 边框/分隔线 | `var(--line)` |
| `#ef4444` `#e11d48` `#f87171` `#EF4444` | 危险/延期/待回款（红） | `var(--danger)`（待回款语义用 `var(--c-remaining)`，延期用 `var(--c-delayed)`） |
| `#10b981` `#34d399` `#10B981` `#34D399` | 已回款（绿） | `var(--c-paid)`（= `var(--ok)`） |
| `#f59e0b` `#fbbf24` `#F59E0B` | 警示/待回款（橙） | `var(--c-pending)`（= `var(--warn)`） |
| `#3b82f6` `#2563eb` | 主题蓝/计划回款 | `var(--accent)`（计划语义用 `var(--c-plan)`） |
| `#6366f1` `#4f46e5` | 靛蓝（按钮/链接/选中） | `var(--accent)` |
| `#eef2ff` `#ede9fe` 等浅色调底 | 主题浅色 hover/选中底 | `color-mix(in srgb, var(--accent) 12%, transparent)` |

语义状态色 token（`--c-paid/--c-pending/--c-remaining/--c-delayed/--c-plan`）与反白色 `--on-accent` 在 Task 1 定义；后续任务直接引用。

---

## 约定（CLAUDE.md，所有任务遵守）

- 简体中文;**无 emoji**，符号用 `→ ↓ ❌ ✕ ▾`。
- 仅改样式/模板可达性，**不改任何业务计算、props 接口、数据口径**。
- 跟进表单只保留 记录编号/项目编号/项目名称 三只读字段，**不引入 amountTier**——本计划不动表单字段，仅换样式。
- 提交信息结尾固定：
  ```
  Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
  ```
- 偏好补 CSS 不引框架（本计划全是 CSS 变量 + 一个微型指令，符合）。

---

### Task 1: theme.css 全局 token 地基（对比度 + 语义状态色 + 反白色 + 自适应工具）

**Files:**
- Modify: `frontend/src/styles/theme.css`

修复 muted 对比度（`--mut` 浅色 2.62:1 → 达标）、补语义状态色与反白色 token、加无断点自适应栅格工具类与断点注释。这是 P1-c + P2-自适应基线。`theme.css` 无单测，验证靠对比度计算 + typecheck/build。

- [ ] **Step 1: 提高 `--mut` 对比度（浅色 + 深色）**

把 `:root` 的 `--mut: #93a1b8;`（白底约 2.62:1，不达标）改为：

```css
  --mut: #64748b;   /* 白底约 4.76:1，达 WCAG AA */
```

把 `html.dark` 的 `--mut: #5d728f;`（深卡底约 3.39:1）改为：

```css
  --mut: #8595ad;   /* #111c30 卡底约 5.48:1，达 WCAG AA */
```

- [ ] **Step 2: 在 `:root` 末尾（`--el-border-radius-base: 8px;` 之后、`}` 之前）追加语义 token**

```css

  /* 语义状态色（回款语义；引用调色板，随主题自动切换） */
  --c-paid: var(--ok);          /* 已回款 */
  --c-pending: var(--warn);     /* 待回款/警示 */
  --c-remaining: var(--danger); /* 待回款缺口 */
  --c-delayed: var(--danger);   /* 延期 */
  --c-plan: var(--accent);      /* 计划回款 */
  /* 彩色填充上的反白前景（文字/图标），两主题恒为白 */
  --on-accent: #ffffff;
```

- [ ] **Step 3: 文件末尾追加自适应栅格工具类 + 断点注释**

在文件最末（滚动条规则之后）追加：

```css

/* 自适应栅格：无断点自动换列，--col-min 控制单列最小宽，供卡片/指标区复用。
   断点约定（页面按需用 @media）：窄屏 <=768px，常规 <=1200px。 */
.u-grid-auto {
  display: grid;
  gap: 12px;
  grid-template-columns: repeat(auto-fit, minmax(var(--col-min, 200px), 1fr));
}
```

- [ ] **Step 4: 类型/构建校验**

Run: `cd frontend && npm run typecheck && npm run build`
Expected: 两者均通过（theme.css 改动不影响类型;build 成功表示 CSS 合法）。

- [ ] **Step 5: 回归现有用 token 的组件测试**

Run: `cd frontend && npm run test:run -- src/stores/settings.test.ts src/components/DisplaySettings.test.ts src/components/ProjectDetailDrawer.test.ts`
Expected: 全部通过（这些组件已吃 token，改 token 值不应破坏测试）。

- [ ] **Step 6: Commit**

```bash
git add frontend/src/styles/theme.css
git commit -m "fix(D2.5): theme.css 修 --mut 对比度 + 语义状态色/反白色 token + 自适应栅格工具(P1-c/P2)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 2: v-activate 全局键盘激活指令（P1-b 地基）

**Files:**
- Create: `frontend/src/directives/activate.ts`
- Create: `frontend/src/directives/activate.test.ts`
- Modify: `frontend/src/main.ts`

提供一个可复用指令：给任意 `@click` 的非语义元素（表格行、富日格等）自动补 `role="button"` + `tabindex="0"`，并在 Enter/Space 时合成一次 `click`，复用元素上已有的 `@click`，无需额外传 handler。后续所有下钻入口统一用它，D3+ 也复用。

- [ ] **Step 1: 写失败测试**

Create `frontend/src/directives/activate.test.ts`:

```ts
import { describe, it, expect, vi } from 'vitest'
import { mount } from '@vue/test-utils'
import { vActivate } from './activate'

const Host = {
  template: `<div class="hit" v-activate @click="onClick">x</div>`,
  setup() {
    const onClick = vi.fn()
    return { onClick }
  },
}

function mountHost() {
  return mount(Host, { global: { directives: { activate: vActivate } } })
}

describe('v-activate', () => {
  it('挂载后补上 role 与 tabindex', () => {
    const w = mountHost()
    const el = w.get('.hit')
    expect(el.attributes('role')).toBe('button')
    expect(el.attributes('tabindex')).toBe('0')
  })

  it('Enter 键触发元素上的 @click', async () => {
    const w = mountHost()
    await w.get('.hit').trigger('keydown', { key: 'Enter' })
    expect((w.vm as unknown as { onClick: ReturnType<typeof vi.fn> }).onClick).toHaveBeenCalledTimes(1)
  })

  it('Space 键触发元素上的 @click', async () => {
    const w = mountHost()
    await w.get('.hit').trigger('keydown', { key: ' ' })
    expect((w.vm as unknown as { onClick: ReturnType<typeof vi.fn> }).onClick).toHaveBeenCalledTimes(1)
  })

  it('不覆盖已有的 role/tabindex', () => {
    const Custom = {
      template: `<div class="hit" role="link" tabindex="2" v-activate @click="() => {}">x</div>`,
    }
    const w = mount(Custom, { global: { directives: { activate: vActivate } } })
    const el = w.get('.hit')
    expect(el.attributes('role')).toBe('link')
    expect(el.attributes('tabindex')).toBe('2')
  })
})
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd frontend && npm run test:run -- src/directives/activate.test.ts`
Expected: FAIL（`./activate` 模块不存在）。

- [ ] **Step 3: 实现指令**

Create `frontend/src/directives/activate.ts`:

```ts
import type { Directive } from 'vue'

// 让任意非语义元素可由键盘激活：补 role/tabindex，Enter/Space 合成一次 click，
// 复用元素自身已绑定的 @click。用于表格行、富日格等下钻入口。
function onKey(e: KeyboardEvent) {
  if (e.key === 'Enter' || e.key === ' ') {
    e.preventDefault()
    ;(e.currentTarget as HTMLElement).click()
  }
}

export const vActivate: Directive<HTMLElement> = {
  mounted(el) {
    if (!el.hasAttribute('role')) el.setAttribute('role', 'button')
    if (!el.hasAttribute('tabindex')) el.setAttribute('tabindex', '0')
    el.addEventListener('keydown', onKey)
  },
  unmounted(el) {
    el.removeEventListener('keydown', onKey)
  },
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd frontend && npm run test:run -- src/directives/activate.test.ts`
Expected: PASS（4 个用例全过）。

- [ ] **Step 5: 全局注册指令**

修改 `frontend/src/main.ts`：在 import 区加入

```ts
import { vActivate } from './directives/activate'
```

并在 `app.mount('#app')` 之前注册（紧跟现有 `app.use(...)` 链之后、`useSettingsStore(pinia).init()` 之前或之后均可）：

```ts
app.directive('activate', vActivate)
```

- [ ] **Step 6: typecheck + 构建**

Run: `cd frontend && npm run typecheck && npm run build`
Expected: 均通过。

- [ ] **Step 7: Commit**

```bash
git add frontend/src/directives/activate.ts frontend/src/directives/activate.test.ts frontend/src/main.ts
git commit -m "feat(D2.5): v-activate 键盘激活指令 + 全局注册(P1-b 地基)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 3: 共享件 token 化（DataTable / ColumnFilter / FilterBar）

**Files:**
- Modify: `frontend/src/components/DataTable.vue:48`
- Modify: `frontend/src/components/ColumnFilter.vue`（`<style>` 内 7 处 + 模板 1 处 v-activate）
- Modify: `frontend/src/layout/FilterBar.vue`（`<style>` 内 2 处）
- Test: `frontend/src/components/DataTable.test.ts`、`ColumnFilter.test.ts`、`frontend/src/layout/FilterBar.test.ts`（已存在，回归）

按"颜色 Token 映射表"替换。具体：

- `DataTable.vue:48`：`color: #94a3b8;` → `color: var(--mut);`
- `ColumnFilter.vue`：`#cbd5e1`(line110,文字)→`var(--mut)`;`#4f46e5`(line115)→`var(--accent)`;`#eef2ff`(line116)→`color-mix(in srgb, var(--accent) 12%, transparent)`;`#0f172a`(line122)→`var(--txt)`;`#94a3b8`(line125)→`var(--mut)`;`#f1f5f9`(line129)→`var(--line)`。
- `FilterBar.vue`：`border-bottom: 1px solid #e2e8f0`→`var(--line)`;`background: #fff`→`var(--card)`;`color: #475569`→`var(--sub)`;`border: 1px solid #cbd5e1`(select)→`var(--line2)`。

- [ ] **Step 1: 替换 DataTable / FilterBar 的 hex**

按上面逐条 Edit `DataTable.vue` 与 `FilterBar.vue`。

- [ ] **Step 2: 替换 ColumnFilter 的 hex**

按上面逐条 Edit `ColumnFilter.vue` 的 `<style scoped>`。

- [ ] **Step 3: 给 ColumnFilter 的可点击选项行挂 v-activate**

`ColumnFilter.vue` 模板里 `.cf-row`（line133 样式对应、带 `cursor: pointer` 与 `@click` 切换选项的那一行元素）加 `v-activate` 指令。注意：`.cf-icon` 是 el-popover 触发器，由 EP 处理，**不要**加。

- [ ] **Step 4: grep 确认无残留 hex**

Run: `rg -n "#[0-9a-fA-F]{3,8}\b|rgba?\(" frontend/src/components/DataTable.vue frontend/src/components/ColumnFilter.vue frontend/src/layout/FilterBar.vue`
Expected: 无输出（全部已 token 化）。

- [ ] **Step 5: 回归测试**

Run: `cd frontend && npm run test:run -- src/components/DataTable.test.ts src/components/ColumnFilter.test.ts src/layout/FilterBar.test.ts`
Expected: 全部通过。

- [ ] **Step 6: Commit**

```bash
git add frontend/src/components/DataTable.vue frontend/src/components/ColumnFilter.vue frontend/src/layout/FilterBar.vue
git commit -m "fix(D2.5): 共享件 DataTable/ColumnFilter/FilterBar token 化 + 列筛选行键盘可达(P1)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 4: 回款台账 token 化 + 键盘可达 + 去 side-stripe（LedgerView / LedgerTable）

**Files:**
- Modify: `frontend/src/views/LedgerView.vue`（19 处 hex;`:164` side-stripe）
- Modify: `frontend/src/components/LedgerTable.vue`（12 处 hex;`.lt-row` v-activate）
- Test: `frontend/src/views/LedgerView.test.ts`、`frontend/src/components/LedgerTable.test.ts`（回归）

- [ ] **Step 1: token 化 LedgerView 与 LedgerTable**

按"颜色 Token 映射表"把两文件 `<style scoped>`（及任何内联 `style="color:#..."`）的全部 hex 替换为对应 token。

- [ ] **Step 2: 移除 LedgerView 的 side-stripe 左边框**

`LedgerView.vue:164` `.tier-card { ... border-left: 3px solid #94a3b8; }`：删除 `border-left` 声明，改为完整细边框 `border: 1px solid var(--line);`（其余 padding/background 不变，`background: #f8fafc` 按映射改 `var(--card2)`）。

- [ ] **Step 3: 给可点击台账行挂 v-activate**

`LedgerTable.vue` 中 `.lt-row`（`cursor: pointer` + `@click` 打开项目详情的行元素）加 `v-activate`。

- [ ] **Step 4: grep 确认无残留 hex 与 side-stripe**

Run: `rg -n "#[0-9a-fA-F]{3,8}\b|rgba?\(|border-left:\s*[2-9]" frontend/src/views/LedgerView.vue frontend/src/components/LedgerTable.vue`
Expected: 无输出。

- [ ] **Step 5: 回归测试**

Run: `cd frontend && npm run test:run -- src/views/LedgerView.test.ts src/components/LedgerTable.test.ts`
Expected: 全部通过。

- [ ] **Step 6: Commit**

```bash
git add frontend/src/views/LedgerView.vue frontend/src/components/LedgerTable.vue
git commit -m "fix(D2.5): 回款台账 token 化 + 行键盘可达 + 去 side-stripe(P1/P2)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 5: 数据管理 token 化 + 键盘可达（DataView / DataQualityTable）

**Files:**
- Modify: `frontend/src/views/DataView.vue`（16 处 hex）
- Modify: `frontend/src/components/DataQualityTable.vue`（5 处 hex;`.dq-cell.clickable` v-activate）
- Test: `frontend/src/views/DataView.test.ts`、`frontend/src/components/DataQualityTable.test.ts`（回归）

> P3「`.dv-bar-fill` 动 width → transform」不在本计划范围（用户只要 P1+P2），留待后续按需处理。

- [ ] **Step 1: token 化两文件**

按映射表替换全部 hex。`DataView.vue:156` `.dv-bar-fill { ... background: #3b82f6; ... }` → `background: var(--accent);`（其余声明不动）。

- [ ] **Step 2: 给可点击质检单元挂 v-activate**

`DataQualityTable.vue` 中 `.dq-cell.clickable`（`cursor: pointer` + `@click` 下钻的单元）加 `v-activate`。

- [ ] **Step 3: grep 确认无残留 hex**

Run: `rg -n "#[0-9a-fA-F]{3,8}\b|rgba?\(" frontend/src/views/DataView.vue frontend/src/components/DataQualityTable.vue`
Expected: 无输出。

- [ ] **Step 4: 回归测试**

Run: `cd frontend && npm run test:run -- src/views/DataView.test.ts src/components/DataQualityTable.test.ts`
Expected: 全部通过。

- [ ] **Step 5: Commit**

```bash
git add frontend/src/views/DataView.vue frontend/src/components/DataQualityTable.vue
git commit -m "fix(D2.5): 数据管理 token 化 + 质检单元键盘可达(P1)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 6: 关于产品 token 化（AboutView）

**Files:**
- Modify: `frontend/src/views/AboutView.vue`（7 处 hex，`:53-59`）
- Test: `frontend/src/views/AboutView.test.ts`（回归）

- [ ] **Step 1: token 化**

按映射表替换：`#0f172a`→`var(--txt)`;`#94a3b8`→`var(--mut)`;`#e2e8f0`→`var(--line)`;`#64748b`→`var(--mut)`（`.about-k` 标签弱化文本）;`#1f2937`→`var(--txt)`;`#f8fafc`→`var(--card2)`;`#334155`→`var(--sub)`。

- [ ] **Step 2: grep 确认无残留 hex**

Run: `rg -n "#[0-9a-fA-F]{3,8}\b|rgba?\(" frontend/src/views/AboutView.vue`
Expected: 无输出。

- [ ] **Step 3: 回归测试**

Run: `cd frontend && npm run test:run -- src/views/AboutView.test.ts`
Expected: 通过。

- [ ] **Step 4: Commit**

```bash
git add frontend/src/views/AboutView.vue
git commit -m "fix(D2.5): 关于产品 token 化(P1)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 7: 临期跟进 A token 化 + 键盘可达（FollowupView / FollowupSignalRow / FollowupExpandModal）

**Files:**
- Modify: `frontend/src/views/FollowupView.vue`（23 处 hex）
- Modify: `frontend/src/components/FollowupSignalRow.vue`（12 处 hex;`.clickable` v-activate）
- Modify: `frontend/src/components/FollowupExpandModal.vue`（16 处 hex）
- Test: 对应三个 `.test.ts`（回归）

- [ ] **Step 1: token 化三文件**

按映射表替换三文件全部 hex（含内联 `style`）。注意彩色底上的 `#fff` 文字 → `var(--on-accent)`，作面/卡背景的 `#fff` → `var(--card)`。

- [ ] **Step 2: 给信号行可点击元素挂 v-activate**

`FollowupSignalRow.vue` 中 `.clickable`（line68 样式，对应带 `@click` 的元素）加 `v-activate`。

- [ ] **Step 3: grep 确认无残留 hex**

Run: `rg -n "#[0-9a-fA-F]{3,8}\b|rgba?\(" frontend/src/views/FollowupView.vue frontend/src/components/FollowupSignalRow.vue frontend/src/components/FollowupExpandModal.vue`
Expected: 无输出。

- [ ] **Step 4: 回归测试**

Run: `cd frontend && npm run test:run -- src/views/FollowupView.test.ts src/components/FollowupSignalRow.test.ts src/components/FollowupExpandModal.test.ts`
Expected: 全部通过。

- [ ] **Step 5: Commit**

```bash
git add frontend/src/views/FollowupView.vue frontend/src/components/FollowupSignalRow.vue frontend/src/components/FollowupExpandModal.vue
git commit -m "fix(D2.5): 临期跟进(视图/信号行/展开弹窗) token 化 + 信号行键盘可达(P1)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 8: 临期跟进 B token 化 + 去 side-stripe + 键盘可达（FuProjectRow / FuNodeTable）

**Files:**
- Modify: `frontend/src/components/FuProjectRow.vue`（6 处 hex;`:56` `:63` side-stripe;可点击元素 v-activate）
- Modify: `frontend/src/components/FuNodeTable.vue`（3 处 hex）
- Test: 对应 `.test.ts`（回归）

- [ ] **Step 1: token 化两文件**

按映射表替换全部 hex。`#ebe7e2`→`var(--line)`;`#f59e0b`（见 Step 2）。

- [ ] **Step 2: 移除 FuProjectRow 的 side-stripe**

`FuProjectRow.vue:56` `.fpr { ... border: 1px solid #ebe7e2; border-left: 4px solid #f59e0b; ... }`：删 `border-left`，整框改 `border: 1px solid var(--line);`。若需保留"待处理"语义强调，改用左内边一个小圆点或背景轻 tint，**不**用彩色粗左边框。
`FuProjectRow.vue:63` `.fpr-nodes { ... border-left: 2px solid #e2e8f0; }`：这是 2px 灰色缩进引导线（非彩色强调），可保留但改 token：`border-left: 2px solid var(--line);`（灰色 ≤2px 的结构性缩进线不属被禁的彩色 side-stripe，允许）。

- [ ] **Step 3: 给可点击项目行/按钮挂 v-activate**

`FuProjectRow.vue` 中带 `@click` 的非 `<button>` 元素加 `v-activate`;若是 `.fpr-btn`（已是 `<button>`）则不需要。

- [ ] **Step 4: grep 确认**

Run: `rg -n "#[0-9a-fA-F]{3,8}\b|rgba?\(|border-left:\s*[3-9]" frontend/src/components/FuProjectRow.vue frontend/src/components/FuNodeTable.vue`
Expected: 无输出（2px 灰线不被该正则命中）。

- [ ] **Step 5: 回归测试**

Run: `cd frontend && npm run test:run -- src/components/FuProjectRow.test.ts src/components/FuNodeTable.test.ts`
Expected: 全部通过。

- [ ] **Step 6: Commit**

```bash
git add frontend/src/components/FuProjectRow.vue frontend/src/components/FuNodeTable.vue
git commit -m "fix(D2.5): 跟进项目行/节点表 token 化 + 去 side-stripe + 键盘可达(P1/P2)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 9: 临期跟进 C token 化 + 键盘可达（FollowupRecords / FollowupRecordForm）

**Files:**
- Modify: `frontend/src/components/FollowupRecords.vue`（18 处 hex;多个 `@click` 元素）
- Modify: `frontend/src/components/FollowupRecordForm.vue`（9 处 hex）
- Test: 对应 `.test.ts`（回归）

- [ ] **Step 1: token 化两文件**

按映射表替换全部 hex。重点：`.fr-addbtn { background: #6366f1; color: #fff; }` → `background: var(--accent); color: var(--on-accent);`;`.fr-hist-btn { border: 1px solid #6366f1; color: #6366f1; background: #fff; }` → 边框/文字 `var(--accent)`、底 `var(--card)`;`.fr-toasts { z-index: 3000; }` 保留（语义层级，不改）。

- [ ] **Step 2: 给非语义可点击元素挂 v-activate**

`FollowupRecords.vue` 中带 `@click` 的非 `<button>` 元素（如 `.fr-link` 若是 `<button>` 则跳过）加 `v-activate`。`.fr-addbtn`/`.fr-hist-btn`/`.fr-link` 若已是 `<button>` 元素则天然可达，无需指令——逐个核对标签，仅对 `<div>/<span>@click` 加。

- [ ] **Step 3: grep 确认无残留 hex**

Run: `rg -n "#[0-9a-fA-F]{3,8}\b|rgba?\(" frontend/src/components/FollowupRecords.vue frontend/src/components/FollowupRecordForm.vue`
Expected: 无输出。

- [ ] **Step 4: 回归测试**

Run: `cd frontend && npm run test:run -- src/components/FollowupRecords.test.ts src/components/FollowupRecordForm.test.ts`
Expected: 全部通过。

- [ ] **Step 5: Commit**

```bash
git add frontend/src/components/FollowupRecords.vue frontend/src/components/FollowupRecordForm.vue
git commit -m "fix(D2.5): 跟进记录/记录表单 token 化 + 键盘可达(P1)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 10: 全量验证 + PROGRESS 更新

**Files:**
- Modify: `PROGRESS.md`

- [ ] **Step 1: 全量验证**

Run: `bash verify.sh`
Expected: `[PASS] verify.sh 全部通过`（py_compile + ruff + pytest + 前端 typecheck/vitest/build 全绿）。

- [ ] **Step 2: 确认留存路径已无硬编码颜色**

Run: `rg -n "#[0-9a-fA-F]{3,8}\b|rgba?\(" frontend/src/views/LedgerView.vue frontend/src/views/DataView.vue frontend/src/views/AboutView.vue frontend/src/views/FollowupView.vue frontend/src/components/LedgerTable.vue frontend/src/components/DataQualityTable.vue frontend/src/components/FollowupSignalRow.vue frontend/src/components/FollowupExpandModal.vue frontend/src/components/FuProjectRow.vue frontend/src/components/FuNodeTable.vue frontend/src/components/FollowupRecords.vue frontend/src/components/FollowupRecordForm.vue frontend/src/components/DataTable.vue frontend/src/components/ColumnFilter.vue frontend/src/layout/FilterBar.vue`
Expected: 无输出。

- [ ] **Step 3: 更新 PROGRESS.md**

在 PROGRESS.md 顶部"最近更新"与 Phase D Backlog 区追加 D2.5 完成记录：标 D2.5 `[x]`，列出修复内容（P1 三条覆盖留存路径 + P2 自适应工具/去 side-stripe）、明确写出"延后文件清单交由 D3–D10 处理"。"最近更新"日期写 2026-06-08。

- [ ] **Step 4: Commit**

```bash
git add PROGRESS.md
git commit -m "docs(D2.5): PROGRESS 记录审计地基修复完成

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## 完成定义

- 留存路径（16 文件）全部 token 化，暗色模式在这些页面正确生效;延后文件清单明确记录在 PROGRESS 供 D3–D10 接手。
- `v-activate` 指令落地并应用于留存路径全部 `div@click` 下钻入口;D3+ 沿用同一模式。
- `--mut` 对比度达 WCAG AA;语义状态色/反白色/自适应工具类就位。
- 留存路径 side-stripe 彩色左边框清除。
- `bash verify.sh` 全绿;`PROGRESS.md` 已更新。

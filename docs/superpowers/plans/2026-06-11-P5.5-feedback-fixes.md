# P5.5 实测反馈修缮 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development or superpowers:executing-plans. Steps use checkbox (`- [ ]`) syntax.

**Goal:** 处理用户对 P2-P5 的实测反馈（2026-06-11）：字号令牌化（L-21 兑现）、总览交互、清单筛选/列、详情页里程碑表与出处标注、分析页维度与交互。版本 V7.4.1。

**结论依据（调查已完成，见 PROGRESS 即将记录）：**
- 字号三档机制=html font-size:var(--fs-base) + rem 令牌；项目域五页硬编码 px 不缩放（根因）。
- 色彩机制无异常（token/双主题/图表 palette 均正常）；BoardView 2 处 hex 留 P6（L-21）。
- 消耗比=PMIS 状态表 核算/总预算（与 delivery_analysis 明细不同源）。
- PMIS 无逐里程碑行；回款清单（项目回款节点（里程碑）清单）字段可支撑里程碑表。
- Insight 零成本新维度：评级/服务组L4/超支/暂停；后端增列候选（项目级别/项目类型/省份/营销一级部门/项目一级分类/主责部门/销售）报用户备选。

## 任务（分级调度：T1-T5 主循环亲做[小改动密集,亲做+测试最快]，T6 sonnet[机械批量]，T7 主循环）

- [ ] **T1 总览**：KPI 6 卡加跳转（在管→/projects、进行中→?projectStatus=实施中、暂停→?paused=yes、高风险→?riskLevel=高、超支→?overspend=yes、回款达成率→/payment）；健康度三档计数行放大（徽章+数字升一级）。测试补 KPI 链接断言。
- [ ] **T2 清单**：6 筛选下拉 placeholder 修复（empty-values 补 ''，与 InsightView 同法）；加「服务组(L4)」列 + L4 筛选下拉（distinctOptions 扩 orgL4）。lib+view 测试。
- [ ] **T3 详情页**：进度里程碑 Tab 加里程碑节点表（page.nodes 复用：节点/计划里程碑日期 expectedMilestoneDate/计划回款日 planDate/是否达成 isMilestoneAchieved/实际日期 actualDate/完成状态 completionStatus）；成本 Tab 两处出处标注（汇总=PMIS 状态表核算/总预算,明细=delivery_analysis.xlsx）。测试补里程碑表断言。
- [ ] **T4 分析**：交叉次维 el-select 改 SegToggle（排除主维选项,点击即选）；新增 4 维（rating 评级/orgL4 服务组/overspend 超支/paused 暂停,空值归一 评级→'无'、bool→'是'/'否'）→ 11 维。lib+view 测试。
- [ ] **T5 杂项**：无（色彩为答疑结论,不改码）。
- [ ] **T6（sonnet）字号令牌化**：项目域视图（OverviewView/ProjectsView/ProjectDetailView/ActivityView/InsightView/EventTimeline/HealthBadge）+ AppSidebar 的 font-size px → rem 令牌。映射：10-12px→var(--fs-1)；13-14px→var(--fs-2)；16px→var(--fs-3)；18-19px→var(--fs-4)；22px→var(--fs-5)。仅动 style 块 font-size 值,其他属性不动；全量测试+typecheck 守护。
- [ ] **T7 收尾**：版本 V7.4.1 + PROGRESS（答疑结论入档/Handoff/backlog：回款数据源迁 PMIS 待出处、Insight 后端增列候选清单、里程碑口径=回款节点清单）+ verify.sh + 终审（diff 范围有限,主循环终读 + 全量门禁）。

# D5.5 打磨：图表暗色 + 字号跨度 + 档位概览重设计 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复目检发现的三处问题：① ECharts 暗色主题不完整（坐标轴/网格/轴标签/tooltip 未适配）;② 字号档位跨度太窄（14/15/16px）导致切换几乎无感;③ 看板首页「金额档位概览」叙事不清，重设计为「三档回款进度条」（每档 完成率 + 已回/计划 + 延期，点档下钻该档项目）。

**Architecture:** ① 给 `echartsTheme` 的明/暗两套主题补 categoryAxis/valueAxis/tooltip 颜色（hex 常量，echarts 配置层，与既有一致）。② 拉大 `settings.FONT_PX` 跨度（13/15/17），rem 令牌随之放大缩小更明显;ECharts 画布文字缩放不在本次范围。③ 重写 `TierStrip` 为三条进度条，点档复用 `BoardDrilldownModal`（组内项目 → D2 详情面板）。计算复用 `computeTierStats`/`groupByProject`，不改算法。

**Tech Stack:** Vue3 `<script setup lang="ts">` + Pinia + vue-echarts + Vitest。

---

## 背景与范围

来源：D5 合并后 /board 与首页目检反馈（2026-06-08）。已与用户确认方向：
- 档位概览 → **三档回款进度条**（用户选定）。
- 回款日历暗色 → **等 D7-D9 整页重写时一并 token 化**（本次不动 CalendarView/CalGrid/CalNodeTable）。

**根因（已核实）：**
- `echartsTheme.ts` 的 `ENT_THEME`/`ENT_THEME_DARK` 仅设全局 `textStyle.color` 与 `legend`，未设坐标轴线/轴标签/网格线/tooltip → 暗色下用 ECharts 默认深灰，看不清。
- `settings.ts` `FONT_PX = {sm:14, md:15, lg:16}`px，rem 令牌（0.8~1.5rem）三档差仅 ±1px，肉眼无感（概览卡全用 rem，技术上在缩放，只是幅度太小）。
- `TierStrip.vue` 现状：段宽=项目数、图例=待回款金额，两个量混用，无明确叙事。

**本计划改/不改：**
- 改：`frontend/src/charts/echartsTheme.ts`、`frontend/src/stores/settings.ts`（+ settings.test）、`frontend/src/components/TierStrip.vue`（+ TierStrip.test 重写）。
- 不改：CalendarView 系（D7-D9）、其它页面字号 px 债（后续统一）、ECharts 画布文字缩放（本次不做）。

## 约定（CLAUDE.md）

- 简体中文;**无 emoji**。CSS 用主题 token;ECharts 配置层颜色用 hex 常量（与既有 echartsTheme 一致，非 CSS 违规）。
- 计算口径忠实复用 `computeTierStats`/`groupByProject`，不改算法。
- 提交信息结尾：
  ```
  Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
  ```

---

### Task 1: echartsTheme — 补全明/暗坐标轴·网格·tooltip

**Files:**
- Modify: `frontend/src/charts/echartsTheme.ts`

无单测（theme 注册有副作用，难断言）;靠 typecheck + build + 目检验证。

- [ ] **Step 1: 替换两套主题注册**

把 `frontend/src/charts/echartsTheme.ts` 中两处 `registerTheme(...)` 调用整体替换为（PALETTE 与 use(...) 注册保持不变）：

```ts
// 'ent'：浅色（默认，沿用旧版主名，避免破坏既有引用/测试）
export const ENT_THEME = 'ent'
registerTheme(ENT_THEME, {
  color: PALETTE,
  backgroundColor: 'transparent',
  textStyle: { fontFamily: 'Inter, "Noto Sans SC", sans-serif', color: '#1f2a3d' },
  title: { textStyle: { color: '#1f2a3d' } },
  legend: { textStyle: { color: '#5b6b85' } },
  categoryAxis: {
    axisLine: { lineStyle: { color: '#d4dbe8' } },
    axisTick: { lineStyle: { color: '#d4dbe8' } },
    axisLabel: { color: '#5b6b85' },
    splitLine: { show: false, lineStyle: { color: '#eef2f8' } },
  },
  valueAxis: {
    axisLine: { lineStyle: { color: '#d4dbe8' } },
    axisTick: { lineStyle: { color: '#d4dbe8' } },
    axisLabel: { color: '#5b6b85' },
    splitLine: { lineStyle: { color: '#e6eaf2' } },
  },
  tooltip: { backgroundColor: '#ffffff', borderColor: '#e6eaf2', textStyle: { color: '#1f2a3d' } },
})

// 'ent-dark'：深色
export const ENT_THEME_DARK = 'ent-dark'
registerTheme(ENT_THEME_DARK, {
  color: PALETTE,
  backgroundColor: 'transparent',
  textStyle: { fontFamily: 'Inter, "Noto Sans SC", sans-serif', color: '#e6edf7' },
  title: { textStyle: { color: '#e6edf7' } },
  legend: { textStyle: { color: '#8aa0c0' } },
  categoryAxis: {
    axisLine: { lineStyle: { color: '#28385a' } },
    axisTick: { lineStyle: { color: '#28385a' } },
    axisLabel: { color: '#8aa0c0' },
    splitLine: { show: false, lineStyle: { color: '#1f2c44' } },
  },
  valueAxis: {
    axisLine: { lineStyle: { color: '#28385a' } },
    axisTick: { lineStyle: { color: '#28385a' } },
    axisLabel: { color: '#8aa0c0' },
    splitLine: { lineStyle: { color: '#1f2c44' } },
  },
  tooltip: { backgroundColor: '#111c30', borderColor: '#1f2c44', textStyle: { color: '#e6edf7' } },
})
```

- [ ] **Step 2: typecheck + 构建 + 现有图表测试回归**

Run: `cd frontend && npm run typecheck && npm run test:run -- src/charts/ChartBox.test.ts && npm run build`
Expected: 均通过（ChartBox 测试不依赖主题细节;build 成功表示主题对象合法）。

- [ ] **Step 3: Commit**

```bash
git add frontend/src/charts/echartsTheme.ts
git commit -m "fix(D5.5): echartsTheme 补全明/暗坐标轴·网格·tooltip 颜色

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 2: settings — 拉大字号档位跨度

**Files:**
- Modify: `frontend/src/stores/settings.ts`
- Test: `frontend/src/stores/settings.test.ts`

- [ ] **Step 1: 改 FONT_PX**

把 `frontend/src/stores/settings.ts` 的：

```ts
export const FONT_PX: Record<FontScale, string> = { sm: '14px', md: '15px', lg: '16px' }
```

改为：

```ts
export const FONT_PX: Record<FontScale, string> = { sm: '13px', md: '15px', lg: '17px' }
```

- [ ] **Step 2: 同步更新 settings.test 断言**

`frontend/src/stores/settings.test.ts`：
- 第 34 行 `expect(...getPropertyValue('--fs-base')).toBe('16px')` → 改为 `toBe('17px')`。
- 第 45 行 `expect(...getPropertyValue('--fs-base')).toBe('14px')` → 改为 `toBe('13px')`。

- [ ] **Step 3: 运行测试**

Run: `cd frontend && npm run test:run -- src/stores/settings.test.ts`
Expected: PASS（4 用例）。

- [ ] **Step 4: Commit**

```bash
git add frontend/src/stores/settings.ts frontend/src/stores/settings.test.ts
git commit -m "fix(D5.5): 拉大字号档位跨度 13/15/17 使切换可感知

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 3: TierStrip 重设计为三档回款进度条

**Files:**
- Modify(重写): `frontend/src/components/TierStrip.vue`
- Modify(重写): `frontend/src/components/TierStrip.test.ts`

每档一条进度条（宽=完成率、色按健康度）+ 已回/计划金额 + 延期数;点档行 `v-activate` 下钻该档项目（复用 `BoardDrilldownModal` → 点项目开 D2 详情面板）。

- [ ] **Step 1: 重写 TierStrip.vue**

整体替换 `frontend/src/components/TierStrip.vue` 为：

```vue
<script setup lang="ts">
import { ref, computed } from 'vue'
import { useFilterStore } from '@/stores/filter'
import { computeTierStats, groupByProject, type ProjectAgg } from '@/lib/dashboardStats'
import { fmtWan, pct } from '@/lib/format'
import { TIERS } from '@/nav'
import BoardDrilldownModal from './BoardDrilldownModal.vue'

const filter = useFilterStore()

const rows = computed(() =>
  TIERS.map((t) => {
    const s = computeTierStats(t.label, filter.filteredNodes) as Record<string, any>
    const expectedWan = s.expectedAmountWan as number
    const actualWan = s.actualAmountWan as number
    return {
      tier: t.label,
      projectCount: s.projectCount as number,
      expectedWan,
      actualWan,
      completion: expectedWan > 0 ? actualWan / expectedWan : 0,
      delayedCount: s.delayedCount as number,
    }
  }),
)

function barColor(r: number): string {
  return r >= 0.8 ? 'var(--c-paid)' : r >= 0.5 ? 'var(--c-pending)' : 'var(--danger)'
}

const drillOpen = ref(false)
const drillTitle = ref('')
const drillProjects = ref<ProjectAgg[]>([])
function openTier(tier: string) {
  drillTitle.value = tier
  drillProjects.value = groupByProject(
    filter.filteredNodes.filter((n) => (n as Record<string, any>).tier === tier),
  )
  drillOpen.value = true
}
defineExpose({ drillOpen })
</script>

<template>
  <div class="tier-strip">
    <div class="ts-head"><h3 class="ts-title">金额档位 · 回款进度</h3></div>
    <div v-for="r in rows" :key="r.tier" v-activate class="ts-row" @click="openTier(r.tier)">
      <span class="ts-name">{{ r.tier }}</span>
      <span class="ts-bar-wrap">
        <span class="ts-bar" :style="{ width: (r.completion * 100).toFixed(0) + '%', background: barColor(r.completion) }" />
      </span>
      <span class="ts-pct" :style="{ color: barColor(r.completion) }">{{ pct(r.completion) }}</span>
      <span class="ts-amt">已回 {{ fmtWan(r.actualWan * 10000) }} / 计划 {{ fmtWan(r.expectedWan * 10000) }} 万</span>
      <span class="ts-delay" :class="{ on: r.delayedCount > 0 }">延期 {{ r.delayedCount }}</span>
    </div>
    <BoardDrilldownModal v-model="drillOpen" :title="drillTitle" :projects="drillProjects" />
  </div>
</template>

<style scoped>
.tier-strip { }
.ts-head { margin-bottom: 10px; }
.ts-title { font-size: var(--fs-3); font-weight: 700; color: var(--txt); margin: 0; }
.ts-row { display: grid; grid-template-columns: 84px 1fr 52px auto auto; gap: 10px; align-items: center; padding: 7px 8px; border-top: 1px solid var(--line); cursor: pointer; border-radius: 6px; }
.ts-row:first-of-type { border-top: none; }
.ts-row:hover { background: var(--card2); }
.ts-name { font-size: var(--fs-2); color: var(--txt); font-weight: 600; }
.ts-bar-wrap { height: 12px; background: var(--card2); border-radius: 6px; overflow: hidden; }
.ts-bar { display: block; height: 12px; border-radius: 6px; }
.ts-pct { font-size: var(--fs-2); font-weight: 700; text-align: right; }
.ts-amt { font-size: var(--fs-1); color: var(--sub); white-space: nowrap; }
.ts-delay { font-size: var(--fs-1); color: var(--mut); white-space: nowrap; }
.ts-delay.on { color: var(--danger); font-weight: 600; }
</style>
```

- [ ] **Step 2: 重写 TierStrip.test.ts**

整体替换 `frontend/src/components/TierStrip.test.ts` 为：

```ts
import { describe, it, expect, beforeEach } from 'vitest'
import { mount } from '@vue/test-utils'
import { setActivePinia, createPinia } from 'pinia'
import TierStrip from './TierStrip.vue'
import { useDataStore } from '@/stores/data'

beforeEach(() => { setActivePinia(createPinia()); localStorage.clear() })

describe('TierStrip', () => {
  it('每档渲染一条进度行并显示完成率', () => {
    const ds = useDataStore()
    ds.data = {
      meta: { lastUpdate: 'x', totalProjects: 0, totalPaymentNodes: 0 }, dashboard: {}, summary: {},
      rawNodes: [
        { projectId: 'P1', tier: '100万以上', isPaymentRelated: true, nodeStatus: '正常实施中', projectAmount: 2000000, expectedPayment: 1000000, actualPayment: 600000, planMonth: '2026-02' },
      ],
      projectOverview: { projects: [], columns: [] },
      naguanMap: {}, naguanExclude: {}, displayColumns: {}, followupRecords: {},
    } as any
    const w = mount(TierStrip, { global: { stubs: { BoardDrilldownModal: true } } })
    expect(w.findAll('.ts-row').length).toBe(3) // TIERS 三档恒在
    expect(w.text()).toContain('100万以上')
    expect(w.text()).toContain('60%') // 600000/1000000
  })

  it('点击档位行打开下钻', async () => {
    const ds = useDataStore()
    ds.data = {
      meta: { lastUpdate: 'x', totalProjects: 0, totalPaymentNodes: 0 }, dashboard: {}, summary: {},
      rawNodes: [
        { projectId: 'P1', tier: '100万以上', isPaymentRelated: true, nodeStatus: '正常实施中', expectedPayment: 1000000, actualPayment: 600000, planMonth: '2026-02' },
      ],
      projectOverview: { projects: [], columns: [] },
      naguanMap: {}, naguanExclude: {}, displayColumns: {}, followupRecords: {},
    } as any
    const w = mount(TierStrip, { global: { stubs: { BoardDrilldownModal: true } } })
    await w.findAll('.ts-row')[0].trigger('click')
    expect((w.vm as any).drillOpen).toBe(true)
  })
})
```

- [ ] **Step 3: 运行测试**

Run: `cd frontend && npm run test:run -- src/components/TierStrip.test.ts`
Expected: PASS（2 用例）。

- [ ] **Step 4: typecheck**

Run: `cd frontend && npm run typecheck`
Expected: 通过。

- [ ] **Step 5: Commit**

```bash
git add frontend/src/components/TierStrip.vue frontend/src/components/TierStrip.test.ts
git commit -m "feat(D5.5): TierStrip 重设计为三档回款进度条 + 点档下钻

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 4: 全量验证 + PROGRESS 更新

**Files:**
- Modify: `PROGRESS.md`

- [ ] **Step 1: 全量验证**

Run: `bash verify.sh`
Expected: `[PASS] verify.sh 全部通过`。

- [ ] **Step 2: 更新 PROGRESS.md**

- 顶部「最近更新」改为 2026-06-08（Plan D5.5 打磨完成）。
- 在 Phase D backlog 的 D5 行之后加一行 `- [x] **D5.5** 目检打磨：echartsTheme 补全明/暗坐标轴·网格·tooltip;字号档位跨度拉大 13/15/17;TierStrip 重设计为三档回款进度条(完成率+已回/计划+延期，点档下钻 BoardDrilldownModal→D2 详情)。回款日历暗色按既定延后 D7-D9。`
- 「会话交接备注」新增 D5.5 段：来源(目检反馈)、三处修复、回款日历暗色与 ECharts 画布字号缩放为已知延后/未做项、下一步 D6/D7。

- [ ] **Step 3: Commit**

```bash
git add PROGRESS.md
git commit -m "docs(D5.5): PROGRESS 记录目检打磨完成

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## 完成定义

- 暗色下图表坐标轴/轴标签/网格/tooltip 清晰可读;字号小/中/大切换在概览卡可感知;首页档位概览为三档进度条、点档下钻该档项目 → D2 详情。
- 回款日历暗色按既定延后 D7-D9（本计划不动）。
- `bash verify.sh` 全绿;`PROGRESS.md` 已更新。
```

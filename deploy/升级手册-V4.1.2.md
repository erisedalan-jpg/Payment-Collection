# 升级手册 —— V4.1.2（成本明细表改表内滚动展示多行；**从 V4.1.1 单版升**）

> 适用：把**已用 /pm 部署在 Ubuntu 服务器**（nginx:80 → app 127.0.0.1:8080，systemd 托管，目录 `/opt/pmplatform`）的 **V4.1.1** 升到 **V4.1.2**。
> 本包是「**纯前端**」增量包——**只换 dist + 重启 app + 浏览器强刷**。**不改后端数据结构、不需点「更新数据」、不需备份任何 data 文件、无新增页面或权限项。**
> 随附最小更新包：`pmplatform-update-V4.1.2.zip`。
> 若现网不是 V4.1.1，请先用对应旧手册升到 V4.1.1，再用本包。

---

## 0. 本次升级包含什么（V4.1.1 → V4.1.2）

| 版本 | 主要内容 | 动数据结构? | 需更新数据? |
|---|---|---|---|
| V4.1.2 | `/insight/costdetail` 项目成本明细表改为**表内固定高度滚动、一次展示多行**（表头冻结，与 `/projects` 一致） | 否（纯前端） | 否 |

具体：
- V4.1.1 曾去掉该明细表的固定表头以修「只显示一行」，但那样表格会按全部行铺满页面、需滚整页查看，不是所需形态。
- V4.1.2 改为与 `/projects` 一致：**表头冻结 + 表内固定高度（约 640px）滚动**，一次可见约 15 行、表内滚动看完整一页（每页 20 行），分页可切 20/50/100。修复的是它此前在长页面底部把表体高度压缩、只露约一行的问题（改用固定高度绕过按位置动态测高的塌缩）。
- 只影响 `/insight/costdetail` 下方「项目成本明细」这一张表；其它页面与该页上方「L4 部门成本情况汇总」不变。

> **本次无「头号注意」级别的坑**：纯前端、无数据迁移、无新页面、无新账号授权项。

---

## 1. 最小更新包内容（`pmplatform-update-V4.1.2.zip`）

解压出顶层目录 `pmplatform-update-V4.1.2/`，含：

- 全部后端根 `*.py`（与 V4.1.1 一致，本版未改后端）
- `frontend/dist/`（**已用 `--base=/pm/` 构建**）—— 本版真正的更新内容
- `requirements.txt`、`deploy/升级手册-V4.1.2.md`、`pmisdata/` 脚本
- **不含** `client/`、`data/`、`input/`、`tests/`、`docs/`、`node_modules/`

---

## 2. 升级步骤（Ubuntu 服务器，约 2 分钟）

```bash
# 0) 上传 pmplatform-update-V4.1.2.zip 到服务器任意临时目录并解压
cd /tmp && unzip -o pmplatform-update-V4.1.2.zip

# 1) 备份 dist(回滚用)
TS=$(date +%Y%m%d-%H%M)
sudo cp -r /opt/pmplatform/frontend/dist /opt/pmplatform/frontend/dist.bak-$TS

# 2) 覆盖前端 dist
cd /tmp/pmplatform-update-V4.1.2
sudo rm -rf /opt/pmplatform/frontend/dist
sudo cp -r frontend/dist /opt/pmplatform/frontend/dist

# 3) (可选)覆盖后端 .py(与现网一致,覆盖无害)
sudo cp -f *.py /opt/pmplatform/

# 4) 重启 app,让其加载新 dist
sudo systemctl restart pmplatform      # 若服务名不同,按现网实际名称
sudo systemctl status pmplatform --no-pager | head -5

# 5) 浏览器强刷(Ctrl+F5),确认左上角版本号 V4.1.2
```

> 注：若 nginx 缓存了静态资源，强刷仍是旧版时 `sudo systemctl reload nginx` 后再刷。
> **本版不需要进 /data 点「更新数据」。**

---

## 3. 升级后验证清单

- [ ] 左上角版本号显示 **V4.1.2**
- [ ] `/insight/costdetail` 下方「项目成本明细」表：**表头冻结**、表内有滚动条，一次可见约 15 行，向下滚可看完整一页（20 行）——与 `/projects` 表格观感一致
- [ ] 排序、列头筛选、导出 Excel、分页（20/50/100）仍正常

> 若该表仍只显示很少行，看表格下方「共 X 条」：显示「共 1 条」或个位数说明是 `/data` 全局「按标签排除」命中了几乎所有项目（去 /data 关掉），与本次显示调整无关。

---

## 4. 已知的行为变化（不是缺陷）

1. **成本明细表表头恢复冻结 + 表内滚动**（V4.1.2）：与 V4.1.1 相比，表头重新固定、表体在固定高度内滚动，观感对齐 `/projects`。

---

## 5. 回滚（回到 V4.1.1）

纯前端回滚，只需还原 dist：

```bash
TS=<你升级时用的时间戳>
sudo rm -rf /opt/pmplatform/frontend/dist
sudo mv /opt/pmplatform/frontend/dist.bak-$TS /opt/pmplatform/frontend/dist
sudo systemctl restart pmplatform
```

> 本版未新增/迁移任何 `data/` 文件，未改数据结构，`input/` 不受影响。回滚后无需点「更新数据」。

---

## 附：下一版增量基线

本包从在线基线 **V4.1.1 增量（单版）**。部署完成后现网即为 **V4.1.2**，下一版更新包从 **V4.1.2** 增量即可。
